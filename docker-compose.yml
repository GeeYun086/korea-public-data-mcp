# 로컬에서 빌드/실행 테스트용 (실제 Claude 연동은 README의 stdio 실행 방식을 사용하세요)
services:
  korea-public-data-mcp:
    build: .
    image: korea-public-data-mcp:local
    env_file:
      - .env
    stdin_open: true
    tty: true
    volumes:
      - ./.cache:/app/.cache
